#include<stdio.h>
#include<conio.h>
int evenodd (int a,int n);
int main()
{int i,n,a[50];
printf("programe to display total no of even and odd no \n enter how many no u r going to enter:: ");
scanf("%d",&n);
printf("enter the numbers::");
for(i=0;i<n;i++)
{
    scanf("%d",&a[i]);
}
int evenodd (a,n)
printf("d,%d",e,o);
}
int evenodd (int a,int n)
{int* e,int * o;
    for(i=0;i<n;i++)
    {
        if(a[i]%2==0)
        {
            e++;
        }
        else o++;
    }


}
