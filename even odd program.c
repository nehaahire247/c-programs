#include<stdio.h>
#include<conio.h>
#include<math.h>
main()
{
int a;
printf("program to check no even or odd");
scanf("%d",&a);
if(a%2==0)
{printf(" given no is even no");}
else
{printf("given no is odd no");
}
}
